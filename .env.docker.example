# Sao chép thành .env (cùng thư mục docker-compose.yml) và sửa giá trị.
# docker compose tự đọc file .env này.

# PostgreSQL
POSTGRES_DB=it_learning
POSTGRES_USER=postgres
POSTGRES_PASSWORD=doi-mat-khau-manh-o-day

# Web
WEB_PORT=3000
JWT_SECRET=chuoi-bi-mat-that-dai-va-ngau-nhien
ADMIN_PASSCODE=1

# Email quên mật khẩu (tùy chọn) — App Password Gmail 16 ký tự
SMTP_USER=
SMTP_PASS=
SMTP_FROM=IT Learning <ban@gmail.com>

# LLM cho chatbot (tùy chọn)
OPENAI_API_KEY=
