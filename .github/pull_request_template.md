## What & why

## Changes

-

## Checklist

- [ ] `cd web && npm run lint && npm run typecheck && npm test` pass ở local (nếu đổi `web/`)
- [ ] `ruff check itlr/ scripts/ tests/ && python -m pytest tests/ -q` pass ở local (nếu đổi `itlr/`)
- [ ] Đã cập nhật/thêm test cho thay đổi
- [ ] Đã cập nhật docs nếu cần
