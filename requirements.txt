# Web API (FastAPI) — GIAO DIỆN CHÍNH, không cần streamlit.
#   Chạy:  uvicorn itlr.api.server:app   (hoặc: python -m itlr.api)
fastapi
uvicorn[standard]

pandas
# pyarrow: cần để unpickle item_list.pkl (DataFrame dùng dtype Arrow-backed).
# Có sẵn trong venv host nên local chạy được, nhưng phải khai báo cho image Docker.
pyarrow
numpy
scikit-learn
nltk
ipykernel
jupyter

# Truy hồi ngữ nghĩa + reranker (Sentence-Transformers / CrossEncoder)
sentence-transformers

# Cào dữ liệu web làm phong phú catalog thật (scripts/scrape/*) — HTTP tĩnh.
# (RSS parse bằng xml.etree thư viện chuẩn; bs4 dùng parser "html.parser" sẵn có -> không cần lxml.)
requests
beautifulsoup4

# LLM cho Chatbot (đều tùy chọn — chỉ cần khi cấu hình API key tương ứng)
anthropic
openai

# Chỉ mục vector ANN (tùy chọn). Nếu môi trường chưa có wheel phù hợp,
# app tự fallback brute-force; có thể thay bằng `hnswlib`.
faiss-cpu

# ── Khung đánh giá / nâng cấp (Trụ cột B–G) — chỉ cần khi chạy scripts/eval/* ──
# Learning-to-Rank (LambdaMART). Đánh giá/kiểm định/đồ thị dùng scipy/matplotlib/shap.
lightgbm
scipy
matplotlib
shap
# Experiment tracking (Trụ cột G) + kiểm thử + đo RSS (tùy chọn)
mlflow
pytest
psutil
