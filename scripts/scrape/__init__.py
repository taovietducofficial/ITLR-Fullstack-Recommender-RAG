"""Bộ cào dữ liệu web làm phong phú catalog THẬT (xem README trong file run_scrape.py).

Scraper chỉ THU THẬP + xuất CSV thô (cột khớp COLUMN_ALIASES của scripts/eval/adapt_real_data.py);
toàn bộ map schema / lọc IT / khử trùng lặp / dịch EN->VI do pipeline sẵn có lo.
"""
